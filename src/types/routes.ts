export const enum MainRoutes {
  Main = "/",
}

export const enum MoviesRoutes {
  Get = "/movies",
  GetById = "/movies/:id",
  Create = "/movies/create",
  Update = "/movies/update/:id",
  Remove = "/movies/delete/:id",
}
